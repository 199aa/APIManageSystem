<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.api.mapper.PermissionMapper">

    <resultMap id="BaseResultMap" type="com.api.model.Permission">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="code" property="code"/>
        <result column="type" property="type"/>
        <result column="parent_id" property="parentId"/>
        <result column="icon" property="icon"/>
        <result column="path" property="path"/>
        <result column="sort_order" property="sortOrder"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <select id="selectAll" resultMap="BaseResultMap">
        SELECT * FROM permission WHERE status = 1 ORDER BY sort_order ASC
    </select>

    <select id="selectByRoleId" resultMap="BaseResultMap">
        SELECT p.* FROM permission p
        INNER JOIN role_permission rp ON p.id = rp.permission_id
        WHERE rp.role_id = #{roleId} AND p.status = 1
        ORDER BY p.sort_order ASC
    </select>

    <select id="selectById" resultMap="BaseResultMap">
        SELECT * FROM permission WHERE id = #{id}
    </select>

    <insert id="insert" parameterType="com.api.model.Permission" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO permission (name, code, type, parent_id, icon, path, sort_order, status)
        VALUES (#{name}, #{code}, #{type}, #{parentId}, #{icon}, #{path}, #{sortOrder}, #{status})
    </insert>

    <update id="update" parameterType="com.api.model.Permission">
        UPDATE permission
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="code != null">code = #{code},</if>
            <if test="type != null">type = #{type},</if>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="icon != null">icon = #{icon},</if>
            <if test="path != null">path = #{path},</if>
            <if test="sortOrder != null">sort_order = #{sortOrder},</if>
            <if test="status != null">status = #{status},</if>
        </set>
        WHERE id = #{id}
    </update>

    <delete id="deleteById">
        DELETE FROM permission WHERE id = #{id}
    </delete>

    <insert id="insertRolePermission">
        INSERT INTO role_permission (role_id, permission_id) VALUES (#{roleId}, #{permissionId})
    </insert>

    <delete id="deleteRolePermissions">
        DELETE FROM role_permission WHERE role_id = #{roleId}
    </delete>

    <insert id="batchInsertRolePermissions">
        INSERT INTO role_permission (role_id, permission_id) VALUES
        <foreach collection="permissionIds" item="permissionId" separator=",">
            (#{roleId}, #{permissionId})
        </foreach>
    </insert>

</mapper>
